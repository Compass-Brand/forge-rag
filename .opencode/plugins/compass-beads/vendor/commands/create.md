---
description: Create a new beads issue
argument-hint: [title]
---

Collect the missing inputs if necessary, then create a beads issue with the shell tool.

Preferred command shape:

```bash
bd create --title "..." --description "..." --type task --priority 2
```

After creation, show the issue id and summarize what was created.
