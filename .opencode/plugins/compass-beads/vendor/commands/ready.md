---
description: Find ready-to-work issues
---

Use the shell tool to run `bd ready`.

Present the ready work clearly:

- issue id
- title
- priority
- type

If no issues are ready, suggest `bd-blocked` or `bd-create`.
