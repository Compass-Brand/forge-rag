---
description: Sync beads state with git
---

Use the shell tool to run:

```bash
bd sync
```

This command is especially relevant before commit/push in repos that use beads session-close discipline.
