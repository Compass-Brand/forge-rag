---
description: List issues with optional filters
argument-hint: [flags]
---

Use the shell tool to run `bd list` with any user-specified flags.

Common examples:

- `bd list --status open`
- `bd list --status in_progress`
- `bd list --priority 1`

Summarize the results instead of pasting long raw output when possible.
