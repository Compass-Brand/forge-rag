---
description: Load beads workflow context into the session
---

Run `bd prime` with the shell tool and summarize the current beads workflow context for the user.

If `bd prime` fails because beads is not installed or not initialized, report that clearly and stop.
