# Retrospective - Epic Completion Review Workflow
name: "retrospective"
description: 'Post-epic review to extract lessons and assess success. Use when the user says "run a retrospective" or "lets retro the epic [epic]"'

config_source: "{project-root}/_bmad/bmm/module.yaml"
user_name: "{config_source}:user_name"
communication_language: "{config_source}:communication_language"
user_skill_level: "{config_source}:user_skill_level"
document_output_language: "{config_source}:document_output_language"
date: system-generated
planning_artifacts: "{config_source}:planning_artifacts"
implementation_artifacts: "{config_source}:implementation_artifacts"
project_knowledge: "{config_source}:project_knowledge"
current_epics_dir: "{config_source}:current_epics_dir"
current_architecture_dir: "{config_source}:current_architecture_dir"
current_prd_dir: "{config_source}:current_prd_dir"
current_story_dir: "{config_source}:current_story_dir"
current_evidence_dir: "{config_source}:current_evidence_dir"
project_context: "**/project-context.md"

installed_path: "{project-root}/_bmad/bmm/4-implementation/bmad-retrospective"
template: false
instructions: "{installed_path}/instructions.md"

required_inputs:
  - agent_manifest: "{project-root}/_bmad/_config/agent-manifest.csv"

# Smart input file references - handles both whole docs and sharded docs
# Priority: Whole document first, then sharded version
# Strategy: SELECTIVE LOAD - only load the completed epic and relevant retrospectives
input_file_patterns:
  epics:
    description: "The completed epic for retrospective"
    whole: "{current_epics_dir}/*.md"
    sharded_index: "{current_epics_dir}/**/index.md"
    sharded_single: "{current_epics_dir}/**/epic-{{epic_num}}.md"
    load_strategy: "SELECTIVE_LOAD"
  previous_retrospective:
    description: "Previous epic's retrospective (optional)"
    pattern: "{current_evidence_dir}/**/epic-{{prev_epic_num}}-retro-*.md"
    load_strategy: "SELECTIVE_LOAD"
  architecture:
    description: "System architecture for context"
    whole: "{current_architecture_dir}/*.md"
    sharded: "{current_architecture_dir}/**/*.md"
    load_strategy: "FULL_LOAD"
  prd:
    description: "Product requirements for context"
    whole: "{current_prd_dir}/*.md"
    sharded: "{current_prd_dir}/**/*.md"
    load_strategy: "FULL_LOAD"
  document_project:
    description: "Brownfield project documentation (optional)"
    sharded: "{project_knowledge}/index.md"
    load_strategy: "INDEX_GUIDED"

# Required files
sprint_status_file: "{current_story_dir}/sprint-status.yaml"
