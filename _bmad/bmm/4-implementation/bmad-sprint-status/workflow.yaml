# Sprint Status - Implementation Tracker
name: sprint-status
description: 'Summarize sprint status and surface risks. Use when the user says "check sprint status" or "show sprint status"'
author: "BMad"

# Critical variables from config
config_source: "{project-root}/_bmad/bmm/module.yaml"
user_name: "{config_source}:user_name"
communication_language: "{config_source}:communication_language"
document_output_language: "{config_source}:document_output_language"
implementation_artifacts: "{config_source}:implementation_artifacts"
current_story_dir: "{config_source}:current_story_dir"

# Workflow components
installed_path: "{project-root}/_bmad/bmm/4-implementation/bmad-sprint-status"
instructions: "{installed_path}/instructions.md"

# Inputs
sprint_status_file: "{current_story_dir}/sprint-status.yaml"

# Smart input file references
input_file_patterns:
  sprint_status:
    description: "Sprint status file generated by sprint-planning"
    whole: "{current_story_dir}/sprint-status.yaml"
    load_strategy: "FULL_LOAD"
