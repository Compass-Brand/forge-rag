# Sync Repositories Checklist

- [ ] Current repo scope is `workspace` or `orchestration`
- [ ] `.gitmodules` was inspected
- [ ] `{planning_repositories_file}` was compared against discovered topology
- [ ] Topology proposal written to `{topology_proposal_file}` before canonical state changed
- [ ] Added, removed, moved, and reparented repos were explicitly reviewed
- [ ] `{planning_repositories_file}` was updated only after approval
- [ ] Sync report written to `{default_output_file}`
