model = "gpt-5.3-codex"
model_reasoning_effort = "high"

[features]
multi_agent = true
apps = true

# Configure MCP servers with local environment variables, never hardcoded secrets.
[mcp_servers.context7]
url = "https://mcp.context7.com/mcp"
http_headers = { "CONTEXT7_API_KEY" = "$CONTEXT7_API_KEY" }
